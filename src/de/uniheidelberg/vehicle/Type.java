package de.uniheidelberg.vehicle;

/*
    In computer programming, an enumerated type is a data type consisting of a
    set of named values called elements, members, enumeral, or enumerators of
    the type. The enumerator names are usually identifiers that behave as constants
    in the language.
    (https://en.wikipedia.org/wiki/Enumerated_type)
 */

public enum Type {
    Land, Water, Air
}
